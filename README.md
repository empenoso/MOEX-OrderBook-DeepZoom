# 📊 MOEX Order Book Deep Zoom Visualizer

**Визуализация стаканов Московской Биржи с интерактивным масштабированием** / **Limit Order Book Visualisation**

[![Python](https://img.shields.io/badge/Python-3.8%2B-blue.svg)](https://www.python.org/)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)
[![Status](https://img.shields.io/badge/Status-Production-brightgreen.svg)]()

## 🎯 Описание проекта

Система для создания высокодетализированных (Ultra-HD) интерактивных визуализаций книги ордеров и ленты сделок с Московской Биржи. Проект использует технологию Deep Zoom для обеспечения плавного просмотра гигантских изображений прямо в браузере.

### Ключевые возможности

- 🔥 **Тепловые карты ликвидности** — визуализация объемов на каждом ценовом уровне
- 📈 **Две проекции рынка** — абсолютные цены + относительное смещение от mid-price
- ⚡ **Лента сделок** — наложение исполненных ордеров с индикацией размера и направления
- 🗓️ **Автоматическая разбивка** — данные разделяются на недельные интервалы для удобства анализа
- 🔍 **Deep Zoom** — интерактивный просмотр изображений размером 12000×9000+ пикселей
- 🚀 **Многопоточная обработка** — параллельная генерация для множества инструментов
- 🌐 **Веб-интерфейс** — готовый HTML-просмотрщик на базе OpenSeadragon

## 📁 Структура проекта

```
MOEX-OrderBook-DeepZoom/
├── main.py                    # Главный пакетный обработчик (orchestrator)
├── orderbook_visualizer.py    # Ядро визуализации (core engine)
├── deepzoom_converter.py      # Конвертер в Deep Zoom формат
├── requirements.txt           # Зависимости проекта
├── npz_data/                  # Исходные данные (*.npz файлы)
├── output_images/             # Сгенерированные PNG изображения
│   └── {TICKER}/              # Папка для каждого инструмента
└── deepzoom_output/           # Deep Zoom тайлы и манифесты
    ├── index.html             # Веб-просмотрщик
    └── {TICKER}/              # Тайлы для каждого инструмента
        ├── *.dzi              # Deep Zoom Image манифесты
        └── *_files/           # Директории с тайлами
```

## 📦 Формат исходных данных

Проект работает с файлами `.npz` (NumPy Archive), содержащими следующие массивы:

* ts.npy - временная шкала (Time Index). Каждая метка времени здесь соответствует одному столбцу в файлах цен и объемов.
* A.npy - Ask (A): заявки на продажу. Row 0 - самая низкая цена. Row 49 - самая высокая (дальняя).
* B.npy - Bid (B): заявки на покупку. Row 0 - самая высокая цена. Row 49 - самая низкая (дальняя).
* vA.npy / vB.npy - матрицы ликвидности (Volumes / Sizes).
Показывают, сколько лотов (или контрактов) лежит на каждом уровне цены в конкретный момент времени.
* T.npy - лента сделок (Trades / Tick Data). Это исполненные ордера.

**⚠️ Важно:** данные Московской Биржи являются платными и не могут быть загружены бесплатно.


## 🚀 Установка

### Требования

- Python 3.8+
- 4+ GB RAM (рекомендуется 16 GB для обработки больших файлов)
- Многоядерный процессор (рекомендуется)

### Установка зависимостей

```bash
# Клонирование репозитория
git clone https://github.com/empenoso/MOEX-OrderBook-DeepZoom.git
cd MOEX-OrderBook-DeepZoom

# Установка библиотек
pip install -r requirements.txt
```

## 📊 Использование

### 1. Подготовка данных

Поместите файлы `*.npz` в директорию `npz_data/`:

```bash
npz_data/
├── SBER.npz
├── GAZP.npz
└── LKOH.npz
```

### 2. Запуск обработки

```bash
python main.py
```

Процесс включает два этапа:

1. **Генерация PNG** — создание тепловых карт (разбивка по неделям)
2. **Конвертация в Deep Zoom** — создание тайлов для интерактивного просмотра

### 3. Просмотр результатов

Запустите локальный HTTP-сервер:

```bash
# Python 3
python -m http.server 8000 --directory deepzoom_output

# Или
cd deepzoom_output
python -m http.server 8000
```

Откройте в браузере: `http://localhost:8000`

## ⚙️ Настройка параметров

### Изменение разрешения изображений

В файле `orderbook_visualizer.py`:

```python
TARGET_WIDTH_PX = 12000      # Ширина в пикселях (по умолчанию: 12000)
PRICE_LEVELS_TOP = 1200      # Уровней цен (верхняя карта)
PRICE_LEVELS_BOTTOM = 500    # Уровней цен (нижняя карта)
```

### Настройка многопоточности

В файле `main.py`:

```python
processor = BatchProcessor(n_workers=4)  # Количество параллельных процессов
```

### Размер тайлов Deep Zoom

В файле `deepzoom_converter.py`:

```python
converter = DeepZoomConverter(
    tile_size=256,    # Размер тайла (по умолчанию: 256)
    quality=85        # Качество JPEG (1-100)
)
```

## 🔬 Технические детали

### Алгоритм визуализации

1. **Биннинг данных** — агрегация временных точек в фиксированное количество бинов для стабилизации разрешения
2. **Логарифмическое масштабирование** — применение `np.log1p()` к объемам для выделения как крупных, так и мелких заявок
3. **Двухуровневая проекция**:
   - Верхняя карта: абсолютные цены (min-max диапазон)
   - Нижняя карта: относительное отклонение от mid-price (±0.5%)
4. **Наложение трейдов** — отрисовка сделок поверх карты ликвидности

### Deep Zoom Pipeline

```
PNG изображение (12000×9000 px)
    ↓
Пирамида уровней (12→11→10→...→0)
    ↓
Нарезка на тайлы 256×256
    ↓
DZI манифест + директория тайлов
    ↓
OpenSeadragon viewer
```

## 📈 Примеры использования

### Анализ конкретного периода

Визуализации автоматически разбиваются по неделям с понедельника по воскресенье.

### Сравнение инструментов

HTML-интерфейс группирует визуализации по тикерам и позволяет быстро переключаться между ними.

### Идентификация аномалий

- **Flash Crashes** — резкие провалы в книге ордеров
- **Spoofing** — появление и исчезновение крупных заявок
- **Накопление объемов** — зоны поддержки/сопротивления

## 🛠️ Разработка

### Архитектура модулей

```
main.py ──────────┐
                  ├──→ orderbook_visualizer.py ──→ PNG
                  ├──→ deepzoom_converter.py ───→ DZI + Tiles
                  └──→ index.html generator
```

### Расширение функционала

Для добавления новых типов визуализаций модифицируйте класс `OrderBookVisualizer` в файле `orderbook_visualizer.py`.

## 🐛 Известные ограничения

- Требует значительных вычислительных ресурсов (CPU + RAM)
- Генерация одной недели данных может занимать 2-5 минут
- Размер выходных данных Deep Zoom может достигать нескольких GB

## 📝 Лицензия

MIT License — см. файл [LICENSE](LICENSE)

## 👤 Автор

**Михаил Шардин**
- Website: [https://shardin.name/](https://shardin.name/)
- GitHub: [@empenoso](https://github.com/empenoso)

---

**Дата последнего обновления:** 9 декабря 2025 года
